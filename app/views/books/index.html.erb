<% @books.each do |book| %>
<div>
  <%= image_tag book.get_profile_image(100,100)  %>
  <p>Title
  <%= link_to book_path(book.id) do%>
  <%= book.title %></p>
  <% end %>
  <p>Opinion <%= book.body %></p>
</div>
<% end %>